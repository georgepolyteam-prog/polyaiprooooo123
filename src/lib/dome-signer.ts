// This file is kept for compatibility but the Dome SDK is no longer used client-side.
// The trading flow now uses edge functions for credential creation and order placement.
// EIP-712 signing is done directly via wagmi's walletClient.signTypedData().

// Re-export nothing - this file is deprecated
export {};
